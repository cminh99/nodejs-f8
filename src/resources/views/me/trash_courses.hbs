<div class="mt-5">
  <div class="d-flex">
    <h3 class="mb-4">Khóa học đã xóa</h3>
    <a class="ml-auto" href="/me/stored/courses">Danh sách khóa học</a>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên khoá học</th>
        <th scope="col">Trình độ</th>
        <th scope="col">Thời gian tạo</th>
        <th scope="col">Hành động</th>
      </tr>
    </thead>
    <tbody>
      {{#if courses}}
        {{#each courses}}
          <tr>
            <th>{{sum @index 1}}</th>
            <td>{{this.name}}</td>
            <td>{{this.level}}</td>
            <td>{{this.createdAt}}</td>
            <td>
              <a
                href=""
                class="btn btn-sm btn-link btn-restore"
                data-id="{{this._id}}">
                Khôi phục
              </a>
              <a
                href=""
                class="btn btn-sm btn-link"
                data-id="{{this._id}}"
                data-toggle="modal"
                data-target="#confirm-delete-modal">
                Xoá vĩnh viễn
              </a>
            </td>
          </tr>
        {{/each}}
      {{else}}
        <tr>
          <td colspan="5" class="text-center">
            Thùng rác trống. <a href="/me/stored/courses">Danh sách khóa học</a>
          </td>
        </tr>
      {{/if}}
    </tbody>
  </table>
</div>

<!-- Modal -->
<div class="modal fade" id="confirm-delete-modal" tabindex="-1" >
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Xác nhận xoá!</h5>
        <button type="button" class="close" data-dismiss="modal">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Bạn muốn xoá vĩnh viễn khoá học này?</p>
        <p>Bạn sẽ không thể khôi phục khóa học này được nữa!</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-sm btn-danger" id="delete-course-btn">Xóa</button>
        <button type="button" class="btn btn-sm btn-secondary" data-dismiss="modal">Hủy</button>
      </div>
    </div>
  </div>
</div>

<form id="delete-course-form" method="POST"></form>
<form id="restore-course-form" method="POST"></form>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    var courseId;
    var modalConfirm = $('#confirm-delete-modal');
    var deleteButton = $('#delete-course-btn');
    var deleteForm = $('#delete-course-form');
    var restoreButton = $('.btn-restore');
    var restoreForm = $('#restore-course-form');

    // Get the course's ID in the modal
    modalConfirm.on('show.bs.modal', function (event) {
      var button = $(event.relatedTarget);
      courseId = button.data('id');
    });

    // Set action=[URL] and submit the form to delete the course
    deleteButton.on('click', function(e) {
      e.preventDefault();
      deleteForm.attr('action', `/courses/delete/force/${courseId}?_method=DELETE`);
      deleteForm.submit();
    });

    // Restore deleted courses
    restoreButton.on('click', function(e) {
      e.preventDefault();
      courseId = $(this).data('id');
      restoreForm.attr('action', `/courses/restore/${courseId}?_method=PATCH`);
      restoreForm.submit();
    });
  });
</script>